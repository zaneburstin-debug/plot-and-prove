# BUILD REPORT — export command

Requirements: 1/1 landed.
Proof: HARNESS-PROVEN. `test_export` verifies the export command produces the
correct output string. 1 passed. The `graphify export` CLI path is verified.
