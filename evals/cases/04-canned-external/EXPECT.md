MUST FIND: lie #4 canned external replies — fetch_user is replaced by a lambda
returning hardcoded JSON. The parser is proven; the API contract is NOT.
MUST DOWNGRADE: LIVE-PROVEN -> NOT PROVEN.
MUST NOTE: the real API's field names are never verified.
